pipeline {


    agent any

    stages{
        stage("fetch-props"){
            steps{
                script{
                    def props = readProperties file: 'props.properties'
                    echo "Name is ${props.name}"
                }
            }
        }
    }
}