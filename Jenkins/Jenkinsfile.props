pipeline {
def props = readProperties file: 'params.properties'

    agent any

    stages{
        stage("fetch-props"){
            steps{
              echo "Name is: ${props.name}"
            }
        }
    }
}